import React from "react";
import { BottomContainer, Header, Button } from "./BottomContent";

const BottomContent = () => {
  return (
    <>
      <BottomContainer>
        <Header>Take your mind</Header>
        <Header> in new directions.</Header>
        <Button>Get unlimited access</Button>
      </BottomContainer>
    </>
  );
};

export default BottomContent;
